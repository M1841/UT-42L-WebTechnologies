Model: openai/gpt-oss-20b:f16
Tooling: anomalyco/opencode

(we implemented the data placeholder and most of the homepage layout and base css, then asked the ai to continue)

[build] Style every component based on its visual description in `concept.txt`. Use CSS custom properties. Follow mobile-first responsive design guidelines. Do not use inline styles. Ensure all existing class names are preserved and extended.

[build] On the Home page, fetch projects from the placeholder `data/data.js` file and display them in a new section. Each project card must include and `<img>` tag and a `<div>` tag containing the title, description and year(s) corresponding to its projects.

[build] On the Team page, format each member's academic titles using `Prof.`, `Eng.` and `PhD`, matching the existing formatting from the Home page member cards.

[build] On the Publications page, add a searchbar and a year range filter using a single slider with two handles for the minimum and maximum years. Also add checkboxes to restrict the searching to "title", "author" and/or "venue" (default to all).

[build] Make sure the slider bounds and visual handles never invert. For the handles, use JS code to snap them back in place whenever they would otherwise move out of bounds.

[build] Each publication now has a `year` field. Update the search logic to include it when filtering data.

(we implemented backend code for data storage, endpoints for all the homepage components and some get methods for the other pages, then asked the ai to continue)

[build] Continue the backend implementation. All services have implemented GET endpoints and the home service has its controller implemented. Add the other controllers and move the publications searching and filtering functionality from the client to the server. Follow the same pattern used in the Home controller.

[build] In every component that fetches data from the backend, use `v-if` directives, like in `Projects.js`, to make sure they don't attempt to render code that access properties of the fetch data while it's not available. By default all data should be `undefined` before it's fetched.

[build] Implement light<->dark theme switching logic on the client side using the built-in HTML `data-theme` attribute. The default theme should refer to the system's preference. The theme should be persisted in `localStorage`.

[build] Add exclusively client-side language switching logic (english <-> romanian). Store the selected locale in `localStorage`. On the backend log the requested language but keep the rest of the logic unchanged, as it will be handled later. By default English should be used.

[build] Update the backend to serve data in the requested language. Keep the existing English data as-is and create a new file for default Romaian data, following the same schema. Use the previously logged `lang` parameter to select which data to serve.

--------------------------------------------------------------------------------------

Model: openai/gpt-oss-20b:f16
Tooling: llama-server

(shared a string with all publications copied directly from the desy pdf to have the ai format it)

Considering the following text [...] extract the publications into an array of JSON objects in the following schema: `{title: string, authors: string[], venue: string, year: number}`. An author should be formatted lastname-comma-space-initial-dot, as in "Doe, J." or "Parker, P.B." for multiple initials. For the venue keep the year, id and other numbers that may appear. The year can be extracted from the publication string if not otherwise specified. Preserve the order as in the original text. Return only the JSON array, with no additional explanations.
