Pages:
  Home
    Navbar
      Logo & pages links
    Picture grid
    Areas of expertise
      3 columns, each containing a relevant stock image, area title and descriptions
      Single column on small screens
    Projects
      Carousel displaying a relevant image with text at the bottom: timeframe, title, description
      Each carousel page is a link to the project’s external page
      Team
        Section header links to Team page
        3 columns: director, professors and PhD students; the latter two are carousels showing only one member at a time
        Each member “card” contains a picture, their name, title and links as icons
        Each column header links to corresponding section in Team page (#director, #professors and #phd-students)
      Publications
        Section header links to Publications page
        Search bar that once typed into expands to reveal the top 5 results
        Each result contains the title, authors and where it was published
        Each result links to the article’s external page
        “View More” button at the bottom, linking to Publications page and applying the current search filter
      Addressed issues
        Horizontal cards with two columns: related image and text (title & bullet point list)
        Layout can alternate across cards (image-text for odds, text-image for evens for example)
      Footer
        Contact details (address, faculty, department, phone, fax, director, email etc)
        Last updated date
    Team
      Same navbar
      Director
        Full width card for Dr. Miclea
        Picture, title, name, links and a carousel of recent articles
      Professors
        Identical cards to home page but all professors are shown; number of columns depends on device width
      PhD students
        Same layout as professors
      Same footer
    Publications
      Same navbar
      Same searchbar but non-expandable, and with added checkboxes to tailor the search to article title, author, publication, and a year range selection
      A higher amount of articles are displayed, paginated if too many (pages of 25 articles for example)
      Each article has its own card instead of all of them being part of the expanded searchbar link on the homepage
      Same footer
